@using ReadingClub.Web.ViewModels.Books
@model IEnumerable<BookViewModel>
@{
    ViewBag.Title = "Books";
}

@if(Model.Count() > 0)
{
    foreach(var book in Model)
    {
        <div>
            <h1>@book.Title</h1>
            <p>@book.Author</p>
        </div>
    }
}
